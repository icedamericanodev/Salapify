# f4.26 All-time money-kept hero, implementation report

Shelf item 1 of 3 from the founder's Money Mindset shelf (2026-08-15): an
all-time total of money avoided, on the reflection screen.

## What changed

- flutter/lib/money/mindset_wins.dart: new pure helper mindsetAllTimeAvoided,
  the same validWinAmount win-amount sum the 30-day snapshot uses, with no date
  window. Returns the peso total and how many wins carried a usable amount.
- lib/screens/mindset_flow.dart: the Step 4 "My 30 days" reflection screen now
  opens with an all-time hero ("Money kept, all time") above the unchanged 30-day
  grid. Shown only when at least one win has a usable amount.
- lib/main.dart: stamp f4.26.

## Money and data

READ-ONLY: it reads data.wins and never adds to a balance. Defining "kept" with
the exact same helper the 30-day figure uses means the all-time total and the
30-day "Spending avoided" can never disagree on what counts. No storage or schema
change. Enhance-not-regress: the existing 30-day grid and its meaning are
untouched; the hero sits above it.

## Validation

- flutter analyze: 0 issues. Full suite green.
- New vectors (mindset_all_time_test): sums across all dates; excludes zero,
  negative, and non-numeric amounts; empty and junk are a clean zero.
- New widget assertion (mindset_flow_test): the hero appears once a win exists.
- Rendered Step 4 dark and looked: "Money kept, all time, P5,000, From 2 choices
  you made on purpose."

## Deferred

Shelf items 2 and 3 (rolling 30-day grid, act-on-a-waiting-item); a sleeker
visual pass; the behavior-scientist's further ideas; the f4.21 Credit
Decision-Score reconciliation.

## Risks

Low. Read-only, reuses the vector-pinned win-amount definition, additive to an
existing screen. Ships over the air.
