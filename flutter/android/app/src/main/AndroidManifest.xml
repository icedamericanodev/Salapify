<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required by the Shorebird updater to download over-the-air patches.
         The app's own money data never leaves the device; this permission
         exists for update delivery, the same trust posture as the RN app. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- App Lock: unlock with the phone's own fingerprint or face through
         local_auth. No biometric data ever leaves the device; Android brokers
         the check and only returns success or failure. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <!-- Reminders: on-device local notifications only (log nudge, payday,
         bills, utang). Nothing is sent anywhere. POST_NOTIFICATIONS is the
         Android 13+ runtime permission; RECEIVE_BOOT_COMPLETED lets the
         scheduled reminders survive a reboot. We use INEXACT alarms, so the
         exact-alarm permissions are deliberately NOT requested. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <application
        android:label="Salapify Preview"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"><!--
        Data stays on the device, full stop. Android Auto Backup is OFF because
        the app holds the whole ledger locally: with backup on, that data
        (including, this release, the plaintext copy kept as a migration
        fallback) would be uploaded to the user's Google Drive, which would
        contradict the encrypted-at-rest promise and the app's on-device
        posture. It would also break a restore onto a new phone, because the
        SQLCipher key lives in the Android Keystore and is NOT backed up, so a
        restored encrypted database could not be opened. The only way data
        leaves this phone is the user's own backup export.

        allowBackup=false is the primary control, but it is not the whole story
        on newer Android. The two rule files are defense in depth AND cover a
        real gap: on Android 12 and newer, allowBackup=false disables cloud
        backup but does NOT stop a device-to-device transfer, which is governed
        by the <device-transfer> section of data_extraction_rules.xml. So:
          - fullBackupContent -> res/xml/backup_rules.xml (Android 11 and lower)
          - dataExtractionRules -> res/xml/data_extraction_rules.xml (Android 12+)
        Both exclude every data domain, so financial data leaves this phone only
        by the user's own backup export, on every Android version. Removing
        either attribute, or flipping allowBackup, reddens
        test/backup_posture_test.dart. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <intent-filter>
                <action android:name="es.antonborri.home_widget.action.LAUNCH" />
            </intent-filter>
        </activity>
        <!-- flutter_local_notifications: deliver a scheduled reminder and
             re-register the schedule after a reboot. -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- The Your Number home screen tile. RemoteViews only: the Kotlin
             reads ten plain strings the app wrote and puts them in TextViews.
             It does no money math and no date math, so it can never disagree
             with the app.

             exported="true" is a risk-adjusted choice, not doctrine. Our own
             update broadcast is explicit, so false would work, but OEM
             launchers have a long history of only honouring exported
             providers. The cost of true being wrong is nil (something could
             ask us to redraw the same strings). The cost of false being wrong
             is a widget that never updates, which costs the founder another
             manual APK install to fix. -->
        <receiver
            android:name=".YourNumberWidget"
            android:label="@string/widget_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/your_number_widget_info" />
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
